# Lakehouse week synthesis

Write your half-page note below. Replace each `TODO` section.

## What is a lakehouse?

TODO: One paragraph — cheap object storage, open table format (Delta), and a compute layer that can run both SQL and Python on the same tables.

## Why we moved dbt to Databricks

TODO: One paragraph — connect your Week 10 `fct_trips` project to this week's port. Mention scale (128M rows), incremental builds, and Unity Catalog.

## One thing that surprised me

TODO: A specific observation from this week (timing numbers, a failing/warning test, catalog permissions, lazy evaluation, etc.).
