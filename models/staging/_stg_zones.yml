version: 2

models:
  - name: stg_zones
    description: One row per TLC taxi zone (265 zones total).
    columns:
      - name: location_id
        description: TLC zone ID.
        # TODO: this is the zone primary key and the target of the relationships test.
        # It must be both unique and never null. Add both generic tests.
      - name: borough
        description: NYC borough (Manhattan, Brooklyn, Queens, Bronx, Staten Island, EWR, Unknown).
